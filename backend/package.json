{
    "name": "haven-backend",
    "version": "0.1.0",
    "private": true,
    "type": "commonjs",
    "main": "lib/index.js",
    "engines": {
        "node": ">=20"
    },
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "start": "firebase emulators:start --only functions,firestore,pubsub",
        "lint": "echo \"Linting not configured yet\""
    },
    "dependencies": {
        "@google-cloud/pubsub": "^4.8.0",
        "express": "^4.19.2",
        "firebase-admin": "^12.7.0",
        "firebase-functions": "^5.1.1",
        "zod": "^3.23.8"
    },
    "devDependencies": {
        "@types/express": "^4.17.21",
        "@types/node": "^22.10.10",
        "typescript": "^5.7.3"
    }
}